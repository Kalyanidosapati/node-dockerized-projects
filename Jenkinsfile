pipeline {
    agent any
    stages {
        stage("Checkout") {
            steps {
                // Checkout the code from the repository
                checkout scm
            }
        }
    }
}
     
